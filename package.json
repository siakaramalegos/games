{
  "name": "games",
  "version": "1.0.0",
  "description": "A boardgame collection website using Eleventy and a non-pro json API version of the BGG api",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production npx eleventy",
    "start": "npx @11ty/eleventy --serve --port=8082"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "netlify-plugin-cache-folder": "^0.0.7",
    "node-fetch": "^2.6.1"
  }
}
